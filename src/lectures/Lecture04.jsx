import Quiz from '../components/Quiz.jsx'
import ModbusFrameBuilder from '../widgets/ModbusFrameBuilder.jsx'

const QUESTIONS = [
  {
    q: '1. У чому різниця між holding registers і input registers Modbus?',
    opts: [
      { t: 'Holding (FC03) читаються й записуються, input (FC04) — лише читаються', ok: true },
      { t: 'Holding зберігають цілі, input — числа з комою', ok: false },
      { t: 'Це те саме, різні лише назви', ok: false },
    ],
    ex: 'Модель даних Modbus має 4 таблиці. Holding registers (FC03/FC06/FC16) — читання й запис, типово уставки й дані. Input registers (FC04) — тільки читання, типово виміри. У лічильниках виміри часто кладуть саме в holding або input залежно від вендора.',
  },
  {
    q: '2. Навіщо в RTU-кадрі CRC-16, а в TCP-кадрі його немає?',
    opts: [
      { t: 'У RTU канал (RS-485) не гарантує цілісність, у TCP її забезпечує сам протокол TCP', ok: true },
      { t: 'CRC у TCP переноситься в поле txid', ok: false },
      { t: 'TCP повільніший, тому CRC вимкнули', ok: false },
    ],
    ex: 'RTU працює по «сирому» послідовному каналу, тож кадр несе власну контрольну суму CRC-16. TCP уже гарантує доставлення й цілісність, тому Modbus TCP додає лише 7-байтовий заголовок MBAP і не дублює CRC.',
  },
  {
    q: '3. Значення напруги 230,5 В передано як float32 у двох регістрах. Клієнт прочитав «сміття» типу 1e-41. Найімовірніша причина:',
    opts: [
      { t: 'Неправильний порядок слів (word order) при склеюванні двох регістрів', ok: true },
      { t: 'Лічильник несправний', ok: false },
      { t: 'Замалий таймаут', ok: false },
    ],
    ex: 'float32 не вміщається в один 16-бітний регістр, його передають парою. Якщо переставити старше й молодше слово (word order), бітова комбінація float різко змінюється — виходить безглузде число. Порядок слів — вендор-специфічний і його треба знати з мапи регістрів.',
  },
  {
    q: '4. Slave повернув відповідь із функцією 0x83 замість 0x03. Що це означає?',
    opts: [
      { t: 'Виняток (exception): старший біт коду функції піднято, далі йде код помилки', ok: true },
      { t: 'Це нормальна відповідь на читання', ok: false },
      { t: 'Slave змінив свою адресу', ok: false },
    ],
    ex: 'При помилці slave повертає код функції з піднятим старшим бітом (0x03 → 0x83) і один байт коду винятку (напр. 0x02 — недопустима адреса регістра). Це не Python-виняток, а штатне повідомлення протоколу; клієнт має його розпізнати (rr.isError()).',
  },
]

export default function Lecture04() {
  return (
    <article>
      <div className="kicker">Лекція 4</div>
      <h1>Протокол Modbus RTU / TCP</h1>

      <div className="goals">
        <b>Цілі заняття</b>
        <ul>
          <li>Знати модель даних Modbus (таблиці, коди функцій) та відмінності RTU і TCP.</li>
          <li>Розбирати структуру кадру ADU/PDU по байтах, включно з CRC та заголовком MBAP.</li>
          <li>Правильно декодувати 32-бітні значення (float/uint) з пар регістрів з урахуванням порядку слів і масштабу.</li>
        </ul>
      </div>

      <h2>1. Modbus як промисловий стандарт</h2>
      <p>
        Modbus — найпоширеніший відкритий протокол польового рівня. Він працює за моделлю <b>клієнт–сервер</b>
        (історично «master–slave»): опитувальний пристрій (УСПД, SCADA) — клієнт, а лічильник — сервер, що відповідає на
        запити. У сегменті RS-485 клієнт лише один, сервери адресуються 1–247. Протокол простий, детермінований і
        реалізований майже в кожному лічильнику — тому з нього починаємо практику.
      </p>

      <h2>2. Модель даних і коди функцій</h2>
      <p>
        Modbus оперує чотирма таблицями об’єктів. Два бітові й два 16-бітні регістрові простори:
      </p>
      <table className="data">
        <thead>
          <tr><th>Таблиця</th><th>Тип</th><th>Доступ</th><th>Типові функції</th></tr>
        </thead>
        <tbody>
          <tr><td>Coils</td><td>1 біт</td><td>читання / запис</td><td>FC01 / FC05, FC15</td></tr>
          <tr><td>Discrete Inputs</td><td>1 біт</td><td>лише читання</td><td>FC02</td></tr>
          <tr><td>Input Registers</td><td>16 біт</td><td>лише читання</td><td>FC04</td></tr>
          <tr><td>Holding Registers</td><td>16 біт</td><td>читання / запис</td><td>FC03 / FC06, FC16</td></tr>
        </tbody>
      </table>
      <p>
        Для обліку головні — <b>регістрові</b> простори. Виміряні величини (напруга, струм, потужність, енергія) вендор
        розміщує в input або holding registers. Один регістр — 16 біт, тож більші значення займають кілька регістрів
        поспіль (див. розділ 4).
      </p>

      <h2>3. Структура кадру: ADU = адреса + PDU + контроль</h2>
      <p>
        Повідомлення Modbus складається з <b>PDU</b> (код функції + дані), однакового для RTU і TCP, та обгортки <b>ADU</b>,
        що відрізняється. У RTU до PDU додаються адреса slave спереду й CRC-16 у кінці; у TCP — 7-байтовий заголовок
        <b> MBAP</b> замість них. Побудуйте кадр і подивіться на байти:
      </p>
      <ModbusFrameBuilder />
      <p>
        Зверніть увагу: у RTU CRC передається <b>молодшим байтом уперед</b>, а в TCP поле <code>length</code> задає
        кількість наступних байтів (unit + PDU). Порт Modbus TCP за замовчуванням — 502.
      </p>

      <h2>4. Кодування значень: регістри, 32 біти, порядок слів, масштаб</h2>
      <p>
        Тут — головне джерело помилок у практиці. Один регістр вміщає ціле 0…65535. Реальні величини кодують так:
      </p>
      <ul>
        <li><b>Масштабований uint16</b>: напр. частота ×100 → 5001 означає 50,01 Гц. Компактно, але обмежений діапазон.</li>
        <li><b>uint32 / int32</b>: два регістри склеюються в 32-бітне ціле (напр. лічильник енергії у Вт·год).</li>
        <li><b>float32 (IEEE-754)</b>: два регістри — одне число з комою (напр. 230,5 В).</li>
      </ul>
      <p>
        При склеюванні двох регістрів критичний <b>порядок слів</b> (word order): яке з двох 16-бітних слів старше. Він
        вендор-специфічний (big-endian / little-endian, іноді ще й перестановка байтів у слові). Помилка порядку не дає
        помилки протоколу — ви отримаєте валідне, але <b>безглузде</b> число. Тому мапу регістрів (адреса, тип, порядок,
        масштаб, одиниці) завжди беруть із документації лічильника. Саме це ми відпрацюємо в ЛР1 і у віджеті-декодері.
      </p>

      <h2>5. Винятки та надійність обміну</h2>
      <p>
        Якщо запит некоректний (недопустима адреса, функція, значення), slave повертає <b>відповідь-виняток</b>: код
        функції з піднятим старшим бітом (0x03 → 0x83) плюс байт коду помилки (напр. 0x02 — illegal data address). Це
        штатне повідомлення протоколу, а не збій зв’язку. Клієнт має відрізняти його від валідних даних
        (<code>rr.isError()</code> у pymodbus) і від <b>таймауту</b> (немає відповіді взагалі — обрив, шум, невірна
        швидкість). Практична надійність будується на таймаутах і повторних запитах, які теж треба закладати в бюджет
        опитування (Лекція 3).
      </p>

      <h2>Ключові терміни</h2>
      <div className="terms">
        <dl className="term"><dt>PDU / ADU</dt><dd>Protocol / Application Data Unit — ядро повідомлення (функція+дані) та повна обгортка кадру (адреса+PDU+контроль).</dd></dl>
        <dl className="term"><dt>MBAP</dt><dd>7-байтовий заголовок Modbus TCP (txid, protocol, length, unit) замість адреси й CRC у RTU.</dd></dl>
        <dl className="term"><dt>Holding / Input registers</dt><dd>16-бітні регістри: holding — читання й запис (FC03), input — лише читання (FC04).</dd></dl>
        <dl className="term"><dt>Порядок слів (word order)</dt><dd>Черговість двох 16-бітних слів у 32-бітному значенні; вендор-специфічний, помилка дає безглузде число.</dd></dl>
        <dl className="term"><dt>Exception response</dt><dd>Відповідь із кодом функції +0x80 і кодом помилки; штатний спосіб slave повідомити про некоректний запит.</dd></dl>
      </div>

      <Quiz questions={QUESTIONS} />

      <p className="footer-note">
        🧪 Закріплення — <b>Лабораторна робота 1</b>: підняти емулятор лічильника на pymodbus, зчитати регістри клієнтом
        і коректно декодувати виміри (float32, uint32, масштаб). Робота виконується повністю на комп’ютері, без фізичного
        стенда.
      </p>
    </article>
  )
}
